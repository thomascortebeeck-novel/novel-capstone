models:
  - name: ga4_purchase_utm
    description: this is a raw table with BigQuery events and their related UTM tags
    columns:
      - name: transaction_key
        tests:
          - unique
          - not_null