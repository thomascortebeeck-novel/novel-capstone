models:
  - name: user
    description: this is a raw table with bubble user information
    columns:
      - name: gender
        tests:
          - accepted_values:
              values: ['male', 'female']